# Claude — Generated Artifacts

This folder contains all files generated by Claude Code for the WiseTransact project.

## Structure

```
claude/
├── project/              # Full Cloudflare Workers backend source
│   ├── src/              # TypeScript handlers, middleware, utils, types
│   ├── migrations/       # D1 SQL migrations (0001–0004)
│   ├── scripts/          # Seed SQL
│   ├── .github/          # GitHub Actions CI/CD pipeline
│   ├── wrangler.toml     # Cloudflare Workers config (dev/staging/prod)
│   ├── .dev.vars.example # Local env template (no real secrets)
│   ├── package.json
│   ├── tsconfig.json
│   └── README.md
│
└── docs/
    └── memory/           # Project documentation & agent architecture
        ├── MEMORY.md
        ├── project_multi_agent_setup.md
        ├── project_multi_agent_workflows.md
        └── reference_design_guideline.md
```

## What was built

- Cloudflare Workers API (TypeScript) with JWT auth, D1 SQLite, KV sessions
- 3 live environments: dev / staging / prod on Cloudflare edge
- GitHub Actions CI/CD: type-check → migrate → deploy per environment
- Multi-agent architecture docs for UI/UX, Frontend, Backend, Analytics agents
